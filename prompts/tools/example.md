# /example

The user wants a concrete example of the current block's main concept.

Provide a step-by-step walkthrough. Label each step clearly.
- If the concept involves math or formulas, show the calculation with actual numbers.
- If the concept is a system design decision, describe a minimal concrete scenario
  with specific technology names (not "Service A calls Service B").
- If the concept is an algorithm, trace it through a small input.

After your response, re-display the current block and the tool menu.
